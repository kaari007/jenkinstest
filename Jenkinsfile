pipeline {
   agent any
   stages {
       stage("init") {
           steps {
               echo "Running test init."
           }
       }
       stage("build") {
           steps {
               echo "Running build stage"
           }
       }
       stage("test") {
           steps {
               echo "Running test stage."
           }
       }
       stage("deploy") {
           steps {
               echo "Running deploy stage."
           }
       }
   }  
}
